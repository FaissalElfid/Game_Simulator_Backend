export interface IBadge {
    title: string;
    description: string;
    pronos: number;
    level: string;
}
export interface IBadgeId {
    id: string;
    title: string;
    description: string;
    pronos: number;
    level: string;
}